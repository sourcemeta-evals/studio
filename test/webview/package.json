{
  "name": "@sourcemeta/studio-test-webview",
  "version": "0.0.0",
  "private": true,
  "description": "Tests for Sourcemeta Studio webview",
  "scripts": {
    "compile": "tsc -p ./",
    "pretest": "npm run compile",
    "test": "mocha --ui tdd ../../build/test/webview/**/*.test.js"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "18.x",
    "mocha": "^11.7.4",
    "typescript": "^5.3.0"
  }
}
