{
  "name": "@sourcemeta/studio-test-webview",
  "version": "0.0.0",
  "private": true,
  "description": "Tests for Sourcemeta Studio webview",
  "scripts": {
    "compile": "tsc -p ./",
    "pretest": "npm run compile",
    "test": "node --test ../../build/test/webview/**/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.3.0"
  }
}
