{
  "name": "@sourcemeta/studio-test-webview",
  "version": "0.0.0",
  "private": true,
  "description": "Tests for Sourcemeta Studio webview components",
  "scripts": {
    "compile": "tsc -p ./",
    "pretest": "npm run compile",
    "test": "mocha --require ts-node/register **/*.test.ts"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "24.x",
    "mocha": "^11.7.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.0"
  }
}
