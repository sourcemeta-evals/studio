{
  "name": "@sourcemeta/studio-test-webview",
  "version": "0.0.0",
  "private": true,
  "description": "Tests for Sourcemeta Studio webview",
  "scripts": {
    "compile:api": "tsc -p tsconfig.webview-api.json",
    "compile": "tsc -p ./",
    "pretest": "npm run compile:api && npm run compile",
    "test": "mocha ../../build/test/webview/**/*.test.js --ui tdd --timeout 10000"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "18.x",
    "mocha": "^11.7.4",
    "typescript": "^5.3.0"
  }
}
